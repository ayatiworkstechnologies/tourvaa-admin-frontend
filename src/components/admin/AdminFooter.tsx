export default function AdminFooter() {
  return (
    <footer className="mt-8 border-t border-dash-border py-5 text-xs font-semibold text-dash-subtle">
      <div className="flex flex-col gap-2 sm:flex-row sm:items-center sm:justify-between">
        <span>Tourvaa Admin Console</span>
        <span>Role based access and operations workspace</span>
      </div>
    </footer>
  );
}
